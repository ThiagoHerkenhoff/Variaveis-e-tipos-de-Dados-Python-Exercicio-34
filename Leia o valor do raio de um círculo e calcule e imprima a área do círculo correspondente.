# 34 - Leia o valor do raio de um círculo e calcule e imprima a área do círculo correspondente.
# A áreado círsulo é pi * raio², considere pi = 3.141592.

r = float(input('Informe o raio do círculo: '))
a = 3.141592 * (r * r)
print(f'A área do círculo é: {a}')
